<!DOCTYPE html>
<html>
    <head>
        <title>سوبر ماركت البحرين| الطلبات</title>
        <link rel="stylesheet" href="./orders.css">
    </head>
    <body>
		<style>
			body {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    flex-direction: row;
}

.card {
    width: 150px;
    height: 150px;
    background-color: rgb(231, 231, 231)
}

header {
    display: flex;
    justify-content: center;
    align-content: center;
    font-size: 50px;
    height: 100px;
    width: 150px;
}

button {
    height: 50px;
    width: 150px;
    padding: 0;
}

img {
    margin: 0;
    width: 50px;
    height: 50px;
}
		</style>
		<% if(orders.length == 0){ %>
			<h1>لا توجد طلبات في الوقت الحالي</h1>
		<% } else { %>
		<% orders.forEach(order => { %>
        <div class="card">
            <header><%= order.unit %></header>
			<a href="/order/asdrepeatasd123/<%= order.id %>"><button><img src="https://icon-library.com/images/details-icon-png/details-icon-png-1.jpg"></button></a>
        </div>
		<% })} %>
    </body>
</html>